#include "sort.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * merge_sort - sort using merge sort
 * @array: array
 * @size: size
 * Return: (Success)
 */
void merge_sort(int *array, size_t size)
{
	merge_sortr(array, size, 0, size - 1);
}

void merge_sortr(int *array, size_t size, size_t l, size_t h)
{

}
